/**
 * Collection & sets.
 *
 * One export, no props. It serves `#/collection`, `#/sets` AND `#/owned` — the
 * first two because they always answered the same question, the third because
 * it is the same collection read by printing instead of by set. Point all three
 * `Screen` names at this component.
 */
export { CollectionScreen } from "./CollectionScreen.tsx";
